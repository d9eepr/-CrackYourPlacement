class Solution {
    public int findDuplicate(int[] nums) {
        while(true)
        {
            int t = nums[0];
            if(nums[nums[0]]==0)
            return nums[0];
           nums[0]=nums[nums[0]];
            nums[t]=0;
        }
      
    }
}
